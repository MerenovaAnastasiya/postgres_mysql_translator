spring:
  application:
    name: mysql-consumer
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: my-12398aa98
    hikari:
      idle-timeout: 10000
      maximum-pool-size: 100
    jdbc-url: jdbc:mysql://localhost:3306/test


kafka:
  default:
    url: localhost:9091
    producer:
      acks: all
  topic:
    create-schema:
      name: create_schema
      consumer:
        group.id: ${spring.application.name}.${kafka.topic.create-schema.name}
    create-table:
      name: create_table
      consumer:
        group.id: ${spring.application.name}.${kafka.topic.create-table.name}
#   название схемы базы данных
    test:
      name: test
      consumer:
        group.id: ${spring.application.name}.${kafka.topic.test.name}

